const {BookingRepository} = require('../repositories');

async function createBooking(data){
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    createBooking
}